apiVersion: v1
kind: Endpoints
metadata:
  name: proxmox-external
  namespace: default # Must match the service's namespace
subsets:
  - addresses:
      - ip: YOUR_PROXMOX_IP # Replace with your Proxmox host's IP address
    ports:
      - port: 8006 # Proxmox default HTTPS port
